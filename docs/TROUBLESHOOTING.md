# Troubleshooting

Run `npm run doctor` first. If Phase 1 is absent, run `npm run import:phase1`. If a model is unreachable, check the configured endpoint and `npm run models:status`. Phase 3–6 being blocked is expected until prerequisites are explicitly complete.
