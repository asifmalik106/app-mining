# Bootstrap

On macOS or Ubuntu, install Node 22.5+ and npm, clone the repository, then run `npm install` and `npm run bootstrap`. Bootstrap creates directories and `.env` if absent, never overwriting an existing configuration. It checks, but does not silently install, system software.
